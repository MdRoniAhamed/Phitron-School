যদি আমরা strstr(text,“a”) এবংstrchr(text, ‘a’) কল করি তাহলে পাথক্য হবে ।

strstr একটি stringএর মধ্যে একটি string কত বার আছে এটা চেক করে ।
strchr একটি string এর মধ্যে একটি অক্ষর করবার আছে সেটা চেক করে।
