#include<bits/stdc++.h>
using namespace std;

//Iterators ->begin(),end(),rbegin(),rend()
//Capacity -> size(), max_size(),empty()
//Element access-> front(), back(), advance()
//Modifiers-> assign(),push_front(), pop_front(),pop_back(), insert(),erase(),clear(),swap(),
int main()
{

    return 0;
}
