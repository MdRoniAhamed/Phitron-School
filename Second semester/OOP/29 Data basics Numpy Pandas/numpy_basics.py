import numpy
py_list = [1,3,56, 76, 6]

np_array = numpy.array([1, 3, 45, 6]) #create array
ten_zeros = numpy.zeros(10)  # create all input 0
fifteen_ones = numpy.ones(15) # create all input 1
sequence = numpy.arange(20)  # create all number sequence 
stepper = numpy.arange(0, 51, 5) # create all number different (start, end, different)
spaced = numpy.linspace(1,100,num=5) # create all number (start, end, need_number)
print(spaced)